spring:
  application:
    name: irden-proxy

proxy:
  listen-host: 0.0.0.0
  listen-port: 7777
  upstream-host: localhost
  upstream-port: 21024

logging:
  level:
    root: INFO
    irden.space.proxy.application: DEBUG
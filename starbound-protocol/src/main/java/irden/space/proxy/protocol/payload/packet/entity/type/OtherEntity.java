package irden.space.proxy.protocol.payload.packet.entity.type;

// We dont need this entityes, so i just put them into a one object
public record OtherEntity(
        Integer entityId
) implements Entity{
}
